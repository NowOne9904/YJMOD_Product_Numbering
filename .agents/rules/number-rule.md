---
trigger: always_on
---

안티그래비티 개발 가이드라인 (System Rule)
[기본 원칙: 코드 보존 및 최소 변경]

이전 문맥 유지: 대화의 이전 맥락과 구현된 핵심 로직을 항상 기억하고 유지해라.

핀셋 수정: 변경을 요청한 부분만 수정해라. 요청하지 않은 기존 코드(CSS 스타일, 변수명, 로직 등)를 임의로 최적화하거나 삭제하지 마라.

전체 코드 보존: 코드를 새로 짤 때 생략(// 기존 코드 동일... 등)하지 말고, 수정된 사항이 반영된 전체 코드를 제공하여 복사 붙여넣기가 가능하게 하라.

[기술 스택 및 스타일]
4. 글래스모피즘 유지: '안티그래비티'의 핵심 테마인 블루 톤의 글래스모피즘(반투명, 블러, 미세한 테두리) 스타일을 모든 UI 수정 시 일관되게 적용하라.
5. PWA 최적화: manifest.json 및 sw.js와의 연결성을 항상 고려하고, 배포 환경(Vercel)에서 즉시 작동 가능한 형태를 유지하라.
6. 변수 명명: 기존에 사용된 클래스 명칭(pwa-banner, pwa-install-btn 등)을 존중하여 충돌을 방지하라.